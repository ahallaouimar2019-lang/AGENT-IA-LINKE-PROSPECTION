<div class="page-content">
  <h1>{{ blog.title }}</h1>
  {% for article in blog.articles %}<p><a href="{{ article.url }}" style="color:var(--ice-deep);font-weight:700">{{ article.title }}</a> — {{ article.published_at | date: '%b %d, %Y' }}</p>{% endfor %}
</div>
