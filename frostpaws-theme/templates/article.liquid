<div class="page-content">
  <h1>{{ article.title }}</h1>
  <p style="color:var(--muted);font-size:13.5px;margin-bottom:20px">{{ article.published_at | date: '%b %d, %Y' }}</p>
  <div>{{ article.content }}</div>
</div>
